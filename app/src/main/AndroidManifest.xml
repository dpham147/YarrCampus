<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.orangecoastcollege.cs273.yarrcampus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".BuildingListActivity" />
        <activity android:name=".BuildingDetailsActivity" />
        <activity android:name=".ProfessorListActivity" />
        <activity android:name=".ProfessorDetailsActivity" />
        <activity android:name=".UtilitySearchActivity" />
        <activity android:name=".QuizActivity"></activity>
    </application>

</manifest>